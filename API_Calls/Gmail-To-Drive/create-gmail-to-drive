Generate python code. The generated code will make claude API call and connect to my gmail. The code should use API key to make the API call. API key should be in the .env file. The code should read my gmail from past 24 hours, categorize and summarize them in a table in a CSV file and write the CSV file in a google drive

Generate python code. The generated code will make claude API call and connect to my gmail. The code should use API key to make the API call. API key should be in the .env file. The code should read my gmail from past 24 hours, categorize and summarize them in a table in a CSV file and write the CSV file in a google drive

https://code.claude.com/docs/en/skills

Run the following in claude ai

Create SKILL.md file for python data checking agent data-checking-agent.py. code should load anthropic key from .env file. The python code should read the dataset into a pandas dataframe and create a summary that includes various metadata including number of rows, column details, number of duplicates, various summary statistics, etc. and a small sample of the rows. Then when the user asks any natural language question about the dataset, the python code should use claude API and the summary to answer the user questions. Keep the answers short and the tone friendly. If the user question cannot be answered, tell the user the reason why it cannot be answered

Create SKILL.md file for python data checking agent data-checking-agent.py. code should load anthropic key from .env file. The python code should read the dataset into a pandas dataframe and create a summary that includes various metadata including number of rows, column details, number of duplicates, various summary statistics, etc. and a small sample of the rows. Then when the user asks any natural language question about the dataset, the python code should use claude API and the summary to answer the user questions. Keep the answers short and the tone friendly. If the user question cannot be answered, tell the user the reason why it cannot be answered


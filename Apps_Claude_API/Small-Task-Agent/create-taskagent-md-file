https://code.claude.com/docs/en/skills

Run the following in claude ai

Create SKILL.md file for python chatbot
Generate `agent.py` — a small Claude-powered task agent that:
- Collects user name + profession at startup (personalized context)
- Accepts multi-line tasks via Ctrl+D
- Plans and executes tasks using tools in an agentic loop
- Uses extended thinking for deep reasoning
- Returns valid JSON output for every step
- Handles API errors gracefully
- Dumps the full session to JSON on exit



